# 기본 파이프라인 예시: Beats 입력 → Elasticsearch/STDOUT 출력
input {
  beats {
    port => 5044
  }
}

filter {
  # 필요한 변환 로직을 여기에 추가하세요.
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "codex-%{+YYYY.MM.dd}"
  }
  stdout {
    codec => rubydebug
  }
}
